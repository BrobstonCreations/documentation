name: Generate PDF Documentation

on:
  push:
    tags:
      - '*'
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Version Number (e.g., 1.0.1)'
        required: true
        default: '1.0.0'

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install md-to-pdf
        run: npm install -g md-to-pdf

      - name: Convert Markdown to PDF
        run: |
          mkdir -p output-pdfs

          # 1. Get the list of files into a variable
          FILES=$(find install-instructions/ -iname "*.md" -type f)

          # 2. Loop through them explicitly
          for file in $FILES; do
            # Extract just the filename without the path or extension
            filename=$(basename "$file")
            filename="${filename%.*}"

            echo "Processing: $file"

            # 3. Run the command once for EACH file found
            md-to-pdf "$file" \
              --launch-options '{ "args": ["--no-sandbox"] }' \
              --output-path "output-pdfs/$filename.pdf"
          done

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          files: output-pdfs/*.pdf
          tag_name: ${{ github.event.inputs.version_number || github.ref_name }}
          name: Release ${{ github.event.inputs.version_number || github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
